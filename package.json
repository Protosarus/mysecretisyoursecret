{
  "name": "mysecretisyoursecret.com",
  "version": "1.0.0",
  "description": "Anonymous secret sharing site with JWT auth and strict privacy.",
  "main": "server.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "prestart": "npm run build",
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node -e \"console.log('better-sqlite3: no manual build step required')\"",
    "postinstall": "npm run build && npm rebuild better-sqlite3 --build-from-source"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.4.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.2",
    "npmlog": "^6.0.2",
    "uuid": "^9.0.1"
  }
}
